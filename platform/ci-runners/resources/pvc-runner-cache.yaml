apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-runner-cache
  labels:
    app.kubernetes.io/name: ci-runner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
