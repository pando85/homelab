apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: gitea-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 1 * * *" # UTC
  claimSelector:
    matchLabels:
      backup: gitea-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
