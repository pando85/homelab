apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: weekly-all-labeled
  namespace: velero
spec:
  schedule: 30 2 * * 2
  template:
    includedNamespaces:
      - "*"
    orLabelSelectors:
      - matchLabels:
          backup: velero
    snapshotVolumes: true
    storageLocation: default
    ttl: 2160h0m0s # 90d
    volumeSnapshotLocations:
      - zfspv-incr
  useOwnerReferencesInBackup: true
