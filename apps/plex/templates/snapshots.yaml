apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: plex-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 1 * * *" # UTC
  claimSelector:
    matchLabels:
      backup: plex-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
