apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: sonarr-zfs-backups
spec:
  retention:
    maxCount: 5
  schedule: "0 1 * * *" # UTC
  claimSelector:
    matchLabels:
      backup: sonarr-zfs
