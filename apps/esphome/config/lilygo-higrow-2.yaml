---
substitutions:
  name: "lilygo-higrow-2"
  friendly_name: "Lilygo Higrow 2"
  project_name: "lilygo.higrow-2"
  project_version: "1.1"
  update_interval: 1min
  loglevel: DEBUG
  moisture_min: "2.82"
  moisture_max: "1.39"
  conductivity_min: "0.075"
  conductivity_max: "0.25"
  # Uncomment run_duration and sleep_duration if you want to use deepsleep
  # set how long to stay awake - NOT less then 10sec
  # run_duration: 11s
  # set how long to sleep in minutes
  # sleep_duration: 60min

packages:
  connection: !include ./packages/connection.yaml
  lilygo-higrow: !include ./packages/lilygo-higrow.yaml
