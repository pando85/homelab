---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: antdroid-restic
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: antdroid-restic
    template:
      data:
        RESTIC_REPOSITORY: '{{`{{ .REPOSITORY_TEMPLATE }}/antdroid`}}'
        RESTIC_PASSWORD: '{{`{{ .RESTIC_PASSWORD }}`}}'
        AWS_ACCESS_KEY_ID: '{{`{{ .AWS_ACCESS_KEY_ID }}`}}'
        AWS_SECRET_ACCESS_KEY: '{{`{{ .AWS_SECRET_ACCESS_KEY }}`}}'
  dataFrom:
    - extract:
        key: /volsync/restic-template
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: antdroid-backup
spec:
  sourcePVC: antdroid-data-zfs
  trigger:
    schedule: "30 1 * * 3" # UTC
  restic:
    pruneIntervalDays: 7
    repository: antdroid-restic
    moverSecurityContext:
      runAsUser: 995
      runAsGroup: 991
      fsGroup: 991
    retain:
      weekly: 1
    copyMethod: Clone
    cacheCapacity: 10Mi
