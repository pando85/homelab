apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: radarr-postgres-backups
spec:
  retention:
    maxCount: 1
  schedule: "0 1 * * *" # UTC
  claimSelector:
    matchLabels:
      cluster-name: radarr-postgres
