---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mosquitto-tls-restic
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: mosquitto-tls-restic
    template:
      data:
        RESTIC_REPOSITORY: '{{`{{ .REPOSITORY_TEMPLATE }}/mosquitto-tls`}}'
        RESTIC_PASSWORD: '{{`{{ .RESTIC_PASSWORD }}`}}'
        AWS_ACCESS_KEY_ID: '{{`{{ .AWS_ACCESS_KEY_ID }}`}}'
        AWS_SECRET_ACCESS_KEY: '{{`{{ .AWS_SECRET_ACCESS_KEY }}`}}'
  dataFrom:
    - extract:
        key: /volsync/restic-template
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: mosquitto-tls-backup
spec:
  sourcePVC: mosquitto-tls-data-zfs
  trigger:
    schedule: "30 1 * * 5" # UTC
  restic:
    pruneIntervalDays: 30
    repository: mosquitto-tls-restic
    moverSecurityContext:
      runAsUser: 995
      runAsGroup: 991
      fsGroup: 991
      fsGroupChangePolicy: "OnRootMismatch"
    retain:
      weekly: 4
    copyMethod: Clone
    cacheCapacity: 100Mi
