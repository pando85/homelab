apiVersion: snapscheduler.backube/v1
kind: SnapshotSchedule
metadata:
  name: jellyfin-zfs-backups
spec:
  retention:
    maxCount: 20
  schedule: "0 3 * * *"
  claimSelector:
    matchLabels:
      backup: jellyfin-zfs
  snapshotTemplate:
    snapshotClassName: zfs-local-dataset
