app-template:
  controllers:
    forkline-web:
      labels:
        backup/retain: quaterly
      replicas: 1
      containers:
        nginx:
          image:
            repository: nginx
            tag: 1.29.5-alpine

          securityContext:
            runAsUser: 0

          resources:
            requests:
              cpu: 10m
              memory: 8Mi
            limits:
              cpu: "1"
              memory: 16Mi
          probes:
            liveness:
              enabled: true
            readiness:
              enabled: true
  defaultPodOptions:
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app.kubernetes.io/instance: forkline-web
            app.kubernetes.io/name: forkline-web
  service:
    forkline-web:
      controller: forkline-web
      ports:
        http:
          port: 80

  ingress:
    external:
      enabled: true
      className: nginx-external
      annotations:
        external-dns.alpha.kubernetes.io/enabled: "true"
        external-dns.alpha.kubernetes.io/target: grigri.cloud
        cert-manager.io/cluster-issuer: letsencrypt-prod-dns
      hosts:
        - host: &host forkline.dev
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: forkline-web
                port: http
      tls:
        - hosts:
            - *host
          secretName: forkline-web-external-tls-certificate

  persistence:
    data:
      enabled: true
      type: configMap
      name: forkline-web-config
      globalMounts:
        - path: /usr/share/nginx/html
    config:
      enabled: true
      type: configMap
      name: nginx-config
      advancedMounts:
        forkline-web:
          nginx:
            - path: /etc/nginx/nginx.conf
              subPath: nginx.conf
