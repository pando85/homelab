apiVersion: monitoring.coreos.com/v1alpha1
kind: ScrapeConfig
metadata:
  name: z800-node-exporter
  labels:
    release: monitoring
spec:
  staticConfigs:
    - targets:
        - z800.grigri:9100
  scrapeInterval: 15s
  relabelings:
    - sourceLabels:
        - __address__
      separator: ":"
      regex: "(.*):(.*)"
      replacement: "${1}"
      targetLabel: instance
      action: replace
    - replacement: node-exporter
      targetLabel: job
      action: replace
    - replacement: z800
      targetLabel: hostname
      action: replace
