---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kanidm-0-certificate
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: kanidm-0-certificate
  data:
    - secretKey: tls.der.b64url
      remoteRef:
        key: /kanidm/kanidm-0-certificate
        property: tls.der.b64url
